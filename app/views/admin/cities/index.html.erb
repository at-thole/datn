<div id="page-content">
  <div class="block full">
    <div class="block-title">
      <h2><strong>Cities</strong></h2>
    </div>
    <div class="row">
      <div class="col-md-8 col-md-offset-2">
        <button type = "button" class = "btn btn-info" data-toggle = "modal"
          data-target = "#addCity">New City</button>
        <hr>
        <div class="table-responsive">
          <table class="table table-vcenter table-striped">
            <tr>
              <th class="text-center">STT</th>
              <th class="text-center">Name</th>
              <th class="text-center">Action</th>
            </tr>
            <% @cities.each_with_index do |city, index| %>
              <tr>
                <td class="text-center"><%= index + 1 %></td>
                <td class="text-center"><%= city.name %></td>
                <td class="text-center">
                  <%= link_to edit_admin_city_path(city),
                    class: "btn btn-default" do %>
                    <i class="fa fa-pencil">Edit</i>
                  <% end %>
                  <%= link_to admin_city_path(city), method: :delete,
                    data: {confirm: "Are you sure ?"}, remote: true, class: "btn btn-danger" do %>
                    <i class="fa fa-times">Delete</i>
                  <% end %>
                </td>
              </tr>
            <% end %>
          </table>
        </div>
        <%= will_paginate %>
        <div id = "addCity" class = "modal fade" role = "dialog">
          <div class = "modal-dialog modal-xs">
            <div class = "modal-content">
              <%= render "admin/cities/form_city" %>
              <div class = "modal-footer">
                <button type = button" class = "btn btn-default"
                  data-dismiss = "modal"><%= t "close" %></button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
